pluginManagement {
    repositories {
        maven { url = 'https://maven.fabricmc.net/' }
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    // This resolves the variable from gradle.properties
    id 'fabric-loom' version "${loom_version}"
}

rootProject.name = "Crappy-Achievements"